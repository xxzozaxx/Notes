#include <sys/socket.h>

int
main()
{
        /**
         * 「 Init the world 」
         */
        int ns; // network socket
        ns = socket(AF_INET, SOCK_STREAM, 0);

        /**
         * Specify an addres for connection
         */

        return 0;
}
